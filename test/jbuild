(jbuild_version 1)

(executable
 ((name check_nic)
  (public_name check_nic)
  (modes (native))
  (libraries (core ixy))
  (modules (check_nic))))

(executable
 ((name uname)
  (public_name uname)
  (modes (native))
  (libraries (ixy))
  (modules (uname))))

(executable
 ((name pci_config)
  (public_name pci_config)
  (modes (native))
  (libraries (ixy))
  (modules (pci_config))))
