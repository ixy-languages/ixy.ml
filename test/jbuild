(jbuild_version 1)

(executable
 ((name check_nic)
  (public_name check_nic)
  (modes (native))
  (libraries (core ixy))
  (modules (check_nic))))

(executable
 ((name memory)
  (public_name memory)
  (modes (native))
  (libraries (core ixy))
  (modules (memory))))

(executable
 ((name mmap)
  (public_name mmap)
  (modes (native))
  (libraries (core ixy))
  (modules (mmap))))

(executable
 ((name uname)
  (public_name uname)
  (modes (native))
  (libraries (ixy))
  (modules (uname))))

(executable
 ((name caml_string)
  (public_name caml_string)
  (modes (native))
  (libraries (ixy))
  (modules (caml_string))))

